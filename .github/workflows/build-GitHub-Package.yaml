name: Build AJAX Application

run-name: "Deploy to @${{ github.ref_name }} by @${{ github.actor }}"

on:  
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    paths:
      - 'Ajax/**/*'
      - '.github/workflows/main_build-ajax.yml'
jobs:  
  build:    
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@v3
        with:
            fetch-depth: 0
      
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v1
        
      - name: Navigate to Workspace
        run: cd $GITHUB_WORKSPACE

      - name: Restore Packages
        run: nuget restore NetGitHubActions-POC.sln -ConfigFile NuGet.Config
        env:
            TELERIK_USERNAME: ${{ secrets.MyTelerikAccountUsername }} 
            TELERIK_PASSWORD: ${{ secrets.MyTelerikAccountPassword }}

      - name: Build AJAX Solution
        run: |
          msbuild.exe NetGitHubActions-POC.sln /t:Restore /p:Configuration=Release /p:RuntimeIdentifier=any
          #dotnet pack NetGitHubActions-POC\NetGitHubActions-POC.csproj --configuration=Release
          #msbuild.exe NetGitHubActions-POC.sln /p:platform="Any CPU" /p:configuration="Release"
      - name: Publish NuGet package
        run: nuget push *.nupkg -ApiKey ${{ secrets.NUGET_API_KEY }} -Source https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}          
      # Publish each built artifact to GitHub Packages
      #- name: Publish to GitHub Packages
      #  uses: actions/upload-artifact@v2
      #  with: 
      #      # Name the artifact based on the project name and configuration
      #      name: NetGitHubActions-POC-Release
      #      # Set the output path to the built DLL file for the project
      #      path: /NetGitHubActions-POC/bin/Release/NetGitHubActions-POC.dll
            
      ## Include the PDB file in the package
      #  if: success()
      #  continue-on-error: true
      #  with:
      #       name: NetGitHubActions-POC-Release-Symbols
      #       path: /MyLibrary1/bin/Release/NetGitHubActions-POC.pdb